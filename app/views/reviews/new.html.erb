<%= form_with model: @review do |f|%>
<%= f.text_area :content %>
<%= f.select :rating, (1..5).to_a %>
<%= f.hidden_field :podcast_id, value: @review.podcast_id %>
<%= f.hidden_field :user_id, value: @review.user_id %>
<%= f.submit "Leave Review!" %>
<% end %>